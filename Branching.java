import Dist.Distribution;
import Int.Distribution;


public class Branching {
  public IntDistribution dist;
  public Branching() {
    dist = new IntDistribution();
  }
  public Branching(IntDistribution d) {
    dist = d;
  }

}
